https://www.amazon.com/<garbage>/zgbs/<garbage>/3753731/ 
- this will always open the correct page irrespective of the values for placeholders, when tried from the browser manually. 
- which means amazon although enforce to have this structure, but it ignores placeholders' values. the node_id is what matters to redirect to right page.
